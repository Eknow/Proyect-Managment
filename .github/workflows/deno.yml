name: Deploy to FTP

on:
    push:
        branches:
            - master

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: Install dependencies and build
              run: |
                  npm cache clean --force
                  npm install --legacy-peer-deps --force
                  npm install env-cmd --legacy-peer-deps
                  npm run build:production

            - name: FTP Deploy
              uses: SamKirkland/FTP-Deploy-Action@4.0.0
              with:
                  server: aguirreteam.projectsutd.online
                  username: u614968815.aguirreteam
                  password: Aguirreteam_2024
                  local-dir: ./build # Ruta local de la carpeta de build
                  remote-dir: /ruta/en/el/servidor # Ruta remota en el servidor FTP
