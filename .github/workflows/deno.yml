name: Deploy to FTP

on:
    push:
        branches:
            - master # Cambia esto según la rama que desees monitorear

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: FTP Deploy
              uses: SamKirkland/FTP-Deploy-Action@4.0.0
              with:
                  server: aguirreteam.projectsutd.online
                  username: u614968815.aguirreteam
                  password: Aguirreteam_2024
                  local-dir: ./path/to/your/files # Ruta local de los archivos que deseas subir
                  remote-dir: /ruta/en/el/servidor # Ruta remota en el servidor FTP
